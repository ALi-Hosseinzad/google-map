import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  root: {
    marginTop: "10%",
  },
  pageContentWrapper: {
    bgcolor: "#cfe8fc",
  },
  grid: {
    textAlign: "end",
  },
  wrapper: {
    display: "flex",
    justifyContent: "space-between",
  },
  action: {
    display: "flex",
    justifyContent: "space-between",
  },
}));
